#include <bits/stdc++.h>
using namespace std;

int main(){
    int n;
    cin >> n;
    vector<int> cubes(n);
    set <int> st;
    for(int i =0 ;i<n; i++){
        cin >> cubes[i];
    }
    for(int i=0; i<n; i++){
        
    }
}